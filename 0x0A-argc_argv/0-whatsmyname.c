#include <stdio.h>
/**
 * main - func name
 * @argc : count of arguments
 * @argv : array of arguments
 * Return: Always 0
 */
int main(int argc, char *argv[])
{
argc += 1;
printf("%s\n", argv[0]);
return (0);
}
