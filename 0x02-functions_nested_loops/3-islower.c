#include<stdio.h>
#include "main.h"
/*
* main - Entry point for this program
* This function prints "_putchar"
* Return: Always 0 (Success)
*/
int _islower(int c)
{
if(islower(c))
{
return (1);
}
return(0);
}
